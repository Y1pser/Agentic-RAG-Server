"""Agentic RAG Server — entry point.

Bootstraps the RAG MCP Server and (optionally) the Agent layer.
"""

import sys


def main() -> int:
    """Main entry point. Placeholder — full server startup in Phase B-4."""
    print("Agentic RAG Server v0.1.0")
    print("RAG MCP Server: not yet started (Phase B)")
    print("Agent Layer: not yet started (Phase C-F)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
